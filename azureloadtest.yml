# YAML file in the main branch

schedules:
- cron: '23 16 * * *'
  displayName: Daily midnight build
  branches:
    include:
    - main
stages:
- stage: Build
  displayName: Build stage
  jobs:
    - deployment: deploy
      displayName: Load Testing
      pool:
        vmImage: ubuntu-latest
      environment: loadtesting
      strategy:
        runOnce:
          deploy:
            steps:
            - checkout: self
              persistCredentials: true
            #  enabled:
              displayName: Git clone source code
<<<<<<< HEAD
            - task: AzureLoadTest1@1  
=======
            - task: AzureLoadTest@1s
>>>>>>> d9fb9d5c684e2395eae0aeb56d3a395be63f3f2b
              displayName: Load Testing
              inputs:
                azureSubscription: loadtestingrest
                loadTestConfigFile: 'loadtest.yaml'
                loadTestResource: MiguelGarcia
                resourceGroup: MiguelGarcia
            - publish: $(System.DefaultWorkingDirectory)/loadTest